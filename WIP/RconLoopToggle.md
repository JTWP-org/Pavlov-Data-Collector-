this is partially done but needs testing there will be a way to trigger a loop of rcon cmds (refresh list and inspect all) used for building a active player data set this can then be used by a mod or bot
the data will be available in the ModSave via a constantly updating json this can be triggered by the discord bot or from inside the modkit by creating a json in the RCON folder 
a planned feature is a ping tracker that will kick if the ping goes to high this will be toggle-able if so u can disable it just like all the features are 
